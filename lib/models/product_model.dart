class Product {
  final String? name, brandName, mainImage, currency, colour, description, stockStatus;
  final int? id, sku, amount;

  Product({
    this.name,
    this.brandName,
    this.mainImage,
    this.currency,
    this.colour,
    this.description,
    this.stockStatus,
    this.id,
    this.sku,
    this.amount,
  });
}
